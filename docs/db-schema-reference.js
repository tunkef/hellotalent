/**
 * HelloTalent — Supabase Veritabanı Şema Referansı
 * ==================================================
 * Bu dosya ÇALIŞTIRILMAZ. Sadece IDE intellisense ve geliştirici referansı içindir.
 * Kolon isimlerini, tiplerini ve ilişkileri burada kontrol et.
 *
 * Son güncelleme: 2026-03-20
 * Kaynak: Codebase audit + Supabase SQL Editor doğrulaması
 *
 * Kullanım:
 *   VS Code'da "Go to Symbol" (Cmd+Shift+O) ile tablo adına atla.
 *   Kolon adını ararken bu dosyada Cmd+F yap.
 */

// ═══════════════════════════════════════════════════
// ANA TABLOLAR
// ═══════════════════════════════════════════════════

/**
 * @typedef {Object} Candidate
 * @property {number} id - bigint, GENERATED BY DEFAULT
 * @property {string} user_id - uuid, FK → auth.users
 * @property {string} full_name
 * @property {string} email
 * @property {string} telefon
 * @property {string} cinsiyet - 'Erkek' | 'Kadın' | 'Belirtmek istemiyorum'
 * @property {number} dogum_yili
 * @property {string} pozisyon
 * @property {number} deneyim_yil
 * @property {string} markalar - comma-separated veya JSON string
 * @property {string} linkedin - URL, https://linkedin.com/in/...
 * @property {string} maas_beklenti
 * @property {string} diller - JSON string
 * @property {string} deneyimler - JSON string (legacy)
 * @property {string} tercih_markalar
 * @property {string} calisma_tipi
 * @property {string} musaitlik - 'Hemen' | '2 Hafta İçinde' | '1 Ay İçinde' | '2+ Ay İçinde'
 * @property {string} segmentler
 * @property {string} tercih_sehirler - JSON
 * @property {string} tercih_ilceler - JSON
 * @property {string} engel_durumu
 * @property {boolean} is_active - Profil görünürlüğü (Beni Öner toggle)
 * @property {boolean} is_actively_looking - Aktif iş arıyor rozeti
 * @property {string} avatar_url
 * @property {string} egitim_seviye
 * @property {number} mezun_yil
 * @property {string} okul
 * @property {string} bolum
 * @property {string} career_goal
 * @property {string} career_type
 * @property {string} cv_url
 * @property {string} cv_filename
 * @property {string} cv_uploaded_at
 * @property {boolean} hide_from_current_employer
 * @property {boolean} profile_completed
 * @property {number} profile_completion_pct
 * @property {string} son_pozisyon - Derived: en güncel deneyimden
 * @property {string} son_sirket - Derived: en güncel deneyimden
 * @property {string} son_marka - Derived: en güncel deneyimden
 * @property {number} toplam_deneyim_ay - Derived: tüm deneyimlerden hesaplanan
 * @property {boolean} halen_calisiyor - Derived: devam eden deneyim var mı
 * @property {string} adres_il
 * @property {string} adres_ilce
 * @property {string} askerlik_durumu
 * @property {boolean} notify_email_messages - default true
 * @property {boolean} notify_email_jobs - default true
 * @property {boolean} notify_sms_enabled - default false (future)
 * @property {boolean} notify_push_enabled - default false (future)
 * @property {string} account_status - 'active' | 'frozen' | 'pending_deletion'
 * @property {string} updated_at - timestamptz
 * @property {string} created_at - timestamptz
 */
var _schemaCandidate;

/**
 * @typedef {Object} CandidateExperience
 * @property {string} id - uuid
 * @property {number} candidate_id - bigint, FK → candidates.id
 * @property {string} sirket - Şirket adı (freeform veya resolved)
 * @property {string} marka - Marka adı (resolved from BRAND_DB veya freeform)
 * @property {string} pozisyon
 * @property {string} baslangic_ay
 * @property {number} baslangic_yil
 * @property {string} bitis_ay
 * @property {number} bitis_yil
 * @property {boolean} devam_ediyor - "Halen burada çalışıyorum"
 * @property {string} rol_ailesi - Rol ailesi kategorisi
 * @property {string} sektor - Sektör
 * @property {number} sira - Sıralama (order)
 * @property {number} company_id - bigint, nullable FK → companies.id (047+)
 * @property {number} brand_id - bigint, nullable FK → brands.id (047+)
 */
var _schemaCandidateExperience;

/**
 * @typedef {Object} CandidateEducation
 * @property {string} id - uuid
 * @property {number} candidate_id - bigint
 * @property {string} egitim_seviye - Eğitim seviyesi
 * @property {string} okul
 * @property {string} bolum
 * @property {number} mezun_yil
 * @property {number} sira
 */
var _schemaCandidateEducation;

/**
 * @typedef {Object} CandidateCertificate
 * @property {string} id - uuid
 * @property {number} candidate_id - bigint
 * @property {string} ad - Sertifika adı
 * @property {string} kurum - Veren kurum
 * @property {number} yil
 * @property {number} sira
 */
var _schemaCandidateCertificate;

/**
 * @typedef {Object} CandidateLanguage
 * @property {string} id - uuid
 * @property {number} candidate_id - bigint
 * @property {string} dil - Dil adı
 * @property {string} seviye - Seviye
 * @property {number} sira
 */
var _schemaCandidateLanguage;

/**
 * @typedef {Object} CandidateTargetRole
 * @property {string} id - uuid
 * @property {number} candidate_id - bigint
 * @property {string} rol_ailesi - Rol ailesi kategorisi (NOT NULL)
 * @property {string} rol_unvani - Rol unvanı (NOT NULL, normalized with titleCaseTR/canonicalizeRole)
 */
var _schemaCandidateTargetRole;

/**
 * @typedef {Object} CandidateWorkPreference
 * @property {string} id - uuid
 * @property {number} candidate_id - bigint
 * @property {string} musaitlik
 * @property {string[]} calisma_tipleri
 * @property {string[]} segmentler
 * @property {string} maas_beklenti
 * @property {string[]} career_types
 * @property {string} career_goal
 */
var _schemaCandidateWorkPreference;

/**
 * @typedef {Object} CandidateBrandInterest
 * @property {string} id - uuid
 * @property {number} candidate_id - bigint
 * @property {string} marka - Tercih edilen marka adı
 * @property {number} brand_id - bigint, nullable FK → brands.id (047+)
 */
var _schemaCandidateBrandInterest;

/**
 * @typedef {Object} CandidateLocationPreference
 * @property {string} id - uuid
 * @property {number} candidate_id - bigint
 * @property {string} sehir
 * @property {CandidateLocationPrefDistrict[]} candidate_location_pref_districts - İlçe relation
 */
var _schemaCandidateLocationPreference;

/**
 * @typedef {Object} CandidateLocationPrefDistrict
 * @property {string} id - uuid
 * @property {string} location_pref_id - FK → candidate_location_preferences.id
 * @property {string} ilce
 */
var _schemaCandidateLocationPrefDistrict;

/**
 * @typedef {Object} CandidateBrandFollow
 * @property {string} id - uuid
 * @property {number} candidate_id - bigint
 * @property {number} brand_id - bigint, FK → brands.id
 * @property {string} created_at - timestamptz
 */
var _schemaCandidateBrandFollow;

/**
 * @typedef {Object} CandidateBlockedCompany
 * @property {string} id - uuid
 * @property {number} candidate_id - bigint
 * @property {number} company_id - bigint, FK → companies.id
 */
var _schemaCandidateBlockedCompany;

// ═══════════════════════════════════════════════════
// ŞİRKET / MARKA TABLOLARI
// ═══════════════════════════════════════════════════

/**
 * @typedef {Object} Company
 * @property {number} id - bigint
 * @property {string} company_name - DİKKAT: "name" değil, "company_name"
 * @property {string} slug
 * @property {string} legal_name
 * @property {string} logo_url
 * @property {string} website_url
 * @property {string} career_url
 * @property {string} linkedin_url
 * @property {string} industry
 * @property {string} segment
 * @property {string} short_description
 * @property {boolean} is_active
 */
var _schemaCompany;

/**
 * @typedef {Object} Brand
 * @property {number} id - bigint
 * @property {number} company_id - bigint, FK → companies.id
 * @property {string} brand_name - Marka adı (Production sütunu: "brand_name")
 * @property {string} slug
 * @property {string} logo_url
 * @property {string} website_url
 * @property {string} instagram_url
 * @property {string} career_url
 * @property {string} short_description
 * @property {string} segment
 * @property {number} store_count_tr
 * @property {string} store_cities
 * @property {string} hq_city
 * @property {number} employee_count_tr
 * @property {boolean} is_featured
 * @property {boolean} is_primary
 * @property {boolean} is_active
 * @property {number} tr_operator_company_id - nullable, Türkiye distribütörü FK → companies.id
 * @property {string} created_at
 */
var _schemaBrand;

// ═══════════════════════════════════════════════════
// İŞVEREN TABLOLARI
// ═══════════════════════════════════════════════════

/**
 * @typedef {Object} HrProfile
 * @property {string} id - uuid, FK → auth.users (DİKKAT: uuid, bigint değil!)
 * @property {string} ad - İsim (first name)
 * @property {string} soyad - Soyisim (last name)
 * @property {string} sirket - Şirket adı (freeform text)
 * @property {string} email
 * @property {string} telefon
 * @property {string} sektor
 * @property {string} buyukluk - Şirket büyüklüğü
 * @property {string} web_sitesi
 * @property {string} segment
 * @property {string} merkez_sehir
 * @property {string} magaza_sayisi
 * @property {string} aciklama
 * @property {string} aranan_profil
 * @property {string} calisma_saatleri
 * @property {string} linkedin
 * @property {number} company_id - bigint, FK → companies.id (016+)
 * @property {string} employer_role - 'admin' | 'recruiter' | 'viewer' (028+, default 'admin')
 * @property {boolean} domain_verified - default false (028+)
 * @property {string} company_type - 'tek_marka' | 'coklu_marka' (028+, default 'tek_marka')
 * @property {string} career_page_url - nullable (030+)
 * @property {number} team_id - bigint, nullable FK → company_teams.id (029+)
 * @property {string} created_at - timestamptz
 *
 * DİKKAT: full_name sütunu YOK. Ad + soyad ayrı alanlar.
 * sender_name derivation: TRIM(COALESCE(ad,'') || ' ' || COALESCE(soyad,''))
 */
var _schemaHrProfile;

/**
 * @typedef {Object} Position
 * @property {number} id - bigint, GENERATED BY DEFAULT
 * @property {string} hr_profile_id - uuid, FK → hr_profiles.id
 * @property {number} company_id - bigint, FK → companies.id
 * @property {string} ad - Pozisyon adı
 * @property {string} sehir - Şehir
 * @property {string} exp - Deneyim beklentisi ("3-5 yıl", "5+ yıl" vb.)
 * @property {string} seg - Segment
 * @property {string} maas - Maaş aralığı
 * @property {string} aciklama - Pozisyon açıklaması
 * @property {string} durum - 'aktif' | 'pasif' | 'taslak'
 * @property {string} gorunum - Görünürlük ayarı
 * @property {string} basvuru - Başvuru yöntemi/linki
 * @property {string} created_at - timestamptz
 */
var _schemaPosition;

// ═══════════════════════════════════════════════════
// RPC FONKSİYONLARI
// ═══════════════════════════════════════════════════

/**
 * Aday profilini tüm child tablolarıyla birlikte atomik kaydet.
 * @function save_candidate_profile
 * @param {Object} p_profile - Candidate tablosu alanları
 * @param {CandidateExperience[]} p_experiences
 * @param {CandidateEducation[]} p_education
 * @param {CandidateCertificate[]} p_certificates
 * @param {CandidateLanguage[]} p_languages
 * @param {CandidateTargetRole[]} p_target_roles
 * @param {CandidateWorkPreference} p_work_prefs
 * @param {{marka: string}[]} p_brand_interests
 * @param {CandidateLocationPreference[]} p_locations
 */
var _rpcSaveCandidateProfile;

/**
 * @function get_employer_team_info
 * @returns {Object} Employer team bilgisi
 */
var _rpcGetEmployerTeamInfo;

/**
 * @function invite_team_member
 * @param {string} p_email
 * @param {string} p_role - 'admin' | 'recruiter' | 'viewer'
 */
var _rpcInviteTeamMember;

/**
 * @function get_employer_plan
 * @returns {string} 'demo' | 'pro' | 'enterprise'
 */
var _rpcGetEmployerPlan;

/**
 * @function check_employer_usage_limit
 * @param {string} p_action - 'candidate_view'
 * @returns {boolean} false ise limit aşıldı
 */
var _rpcCheckEmployerUsageLimit;

/**
 * @function increment_employer_usage
 * @param {string} p_action - 'candidate_view'
 */
var _rpcIncrementEmployerUsage;

/**
 * @function send_employer_message
 * @param {number} p_candidate_id
 * @param {string} p_subject
 * @param {string} p_body
 * @param {number|null} p_position_id
 * @param {number|null} p_template_id
 */
var _rpcSendEmployerMessage;

/**
 * @function link_employer_to_company
 * @param {string} p_company_name
 * @param {string|null} p_website_url
 * @param {string|null} p_sektor
 * @param {string|null} p_segment
 */
var _rpcLinkEmployerToCompany;

/**
 * @function sync_company_details
 * hr_profiles alanlarını companies tablosuna push eder.
 */
var _rpcSyncCompanyDetails;

// ═══════════════════════════════════════════════════
// STORAGE
// ═══════════════════════════════════════════════════

/**
 * @typedef {Object} StoragePaths
 * @property {string} bucket - 'cvs'
 * @property {string} avatarPattern - 'avatars/{user_id}.{ext}'
 * @property {string} cvPattern - '{user_id}/{timestamp}_{filename}'
 */
var _storageConfig;

// ═══════════════════════════════════════════════════
// ═══════════════════════════════════════════════════
// KOC (COACH) SİSTEMİ — Migration 058
// ═══════════════════════════════════════════════════

/**
 * @typedef {Object} CoachInvite
 * @property {number} id - bigint, GENERATED BY DEFAULT
 * @property {string} email
 * @property {string} [display_name]
 * @property {string} invited_by - uuid, FK → admin_users(id)
 * @property {string} status - 'pending' | 'accepted' | 'expired' | 'revoked'
 * @property {string} token - uuid, unique invite URL token
 * @property {string} created_at - timestamptz
 * @property {string} expires_at - timestamptz (default: +30 days)
 * @property {string} [accepted_at] - timestamptz
 * @property {string} [revoked_at] - timestamptz
 *
 * RLS: admin only (is_admin()). Coach can read own invite by email match.
 */

/**
 * @typedef {Object} CoachProfile
 * @property {string} id - uuid PK, FK → auth.users(id)
 * @property {number} invite_id - bigint, FK → coach_invites(id)
 * @property {string} display_name
 * @property {string} [title] - short bio, e.g. "Perakende IK Danismani"
 * @property {string} [avatar_url]
 * @property {boolean} is_active - default true
 * @property {string} created_at - timestamptz
 *
 * RLS: coach own, admin all, authenticated read active.
 */

/**
 * @typedef {Object} CoachPost
 * @property {number} id - bigint, GENERATED BY DEFAULT
 * @property {string} coach_id - uuid, FK → coach_profiles(id)
 * @property {string} title
 * @property {string} [excerpt] - ~200 char preview
 * @property {string} body - full content
 * @property {string} category - 'mulakat_ipucu' | 'yetkinlik_rehberi' | 'kariyer_hikaye' | 'sektor_analiz'
 * @property {string} [related_role] - maps to ROLE_COMP_MAP keys
 * @property {string} [related_competency_code] - maps to competency codes (ao, bi, etc.)
 * @property {string} status - 'draft' | 'submitted' | 'changes_requested' | 'published' | 'archived' | 'rejected'
 * @property {string} [admin_note] - admin feedback
 * @property {number} like_count - denormalized, default 0
 * @property {string} [published_at] - timestamptz
 * @property {string} created_at - timestamptz
 * @property {string} updated_at - timestamptz
 *
 * RLS: coach own (draft/submitted), admin all, authenticated read published.
 * Indexes: (status, published_at DESC), coach_id, status
 */

/**
 * @typedef {Object} CoachPostLike
 * @property {number} candidate_id - bigint, FK → candidates(id) ON DELETE CASCADE
 * @property {number} post_id - bigint, FK → coach_posts(id) ON DELETE CASCADE
 * @property {string} created_at - timestamptz
 *
 * PK: (candidate_id, post_id)
 * RLS: candidate own only (get_my_candidate_id()). Admin can read all.
 * Privacy: candidate sees own like + total count. Cannot see other candidates.
 */

/**
 * RPC: accept_coach_invite(p_token uuid) → json (coach_profiles row)
 *   SECURITY DEFINER. Validates token + email match. Creates coach profile.
 *
 * RPC: toggle_coach_post_like(p_post_id bigint) → integer (new like_count)
 *   SECURITY DEFINER. Atomic like/unlike with denormalized count.
 */

// SIK YAPILAN HATALAR (bu dosyanın var olma sebebi)
// ═══════════════════════════════════════════════════

/**
 * DİKKAT: brands sütunu "brand_name" (name DEĞİL!) — production schema ile doğrulandı (012'den sonra rename edilmiş)
 * HATALI: companies.name       → DOĞRU: companies.company_name
 * HATALI: candidates.id = uuid → DOĞRU: candidates.id = bigint
 * HATALI: hr_profiles.id = int → DOĞRU: hr_profiles.id = uuid
 * HATALI: hr_profiles.full_name → DOĞRU: hr_profiles.ad + hr_profiles.soyad (ayrı alanlar!)
 * HATALI: positions.id = uuid  → DOĞRU: positions.id = bigint
 * HATALI: positions.title      → DOĞRU: positions.ad
 * HATALI: .single()            → DOĞRU: .maybeSingle() (yeni kullanıcılar için)
 * HATALI: canonicalizeRole()   → string döndürmez, {display, canonical} objesi döndürür
 */
