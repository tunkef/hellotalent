# Pending Approvals Arşivi — 2026 Nisan

> chief-of-staff onaylanan/reddedilen approval'ları buraya taşır.
> `.claude/agent-memory/pending-approvals.md` sadece **aktif** onayları tutar.

---

## A4: [Architecture karar] autopilot + autonomous-loop + setup-launchd retire ✅ ONAYLANDI

- **Tarih:** 2026-04-22
- **Tier:** T4
- **Özet:** `scripts/autopilot.sh` + `scripts/autonomous-loop.sh` + `scripts/setup-launchd.sh` archive-20260422/'ye taşındı. Studio v3'te chief-of-staff + Agent Teams peer chat + watchdog agent tüm işlevini karşılıyor.
- **Karar:** [x] Retire (Tuna 22 Nisan 2026 onayı, `cook hellotalent` session — "yeni studio'muzda yok artık, ona gerek de yok").
- **Not:** `.autopilot.*` + `.autonomous-loop.*` state dosyaları gitignore'da, local dead state temizlenebilir. Launchd servisi kuruluysa `launchctl unload ~/Library/LaunchAgents/ai.hellotalent.autopilot.plist` gerekli — kullanıcı manuel çalıştıracak.
- **Kapanış:** 2026-04-30 — chief-of-staff cleanup, retire tamamlandı.

---

## A6: [Migration dry-run + Architecture karar] Phase A — Sprint 7 SQL fix + remote apply ✅ TAMAMLANDI

- **Tarih:** 2026-04-26 12:00
- **Tier:** T3
- **Kapanış:** 2026-04-26 commit `4fd1dcd` — Wave 1-8 yeşil + remote apply SUCCESS.

### Karar Akışı (Wave 1-8, ~3 saat)
- **W1 supabase-agent:** 9 nokta `uuid → bigint` fix
- **W2 auditor + code-reviewer paralel:** convergent 1 BLOCKER + 1 HIGH + 1 MEDIUM
  - BLOCKER: line 288 `GRANT EXECUTE ON FUNCTION hr_get_pipeline(uuid)` — function bigint
  - HIGH: `pipeline_state_update_team` `WITH CHECK` eksik (cross-company inject riski)
  - MEDIUM: `trg_set_updated_at` `SET search_path = public` eksik
- **W3 supabase-agent re-fix:** 7 satır + header doc + test snippet + ON DELETE
- **W4 re-audit:** auditor PASS, code-reviewer hayal-FAIL (`c.sehir` column iddia etti, live schema'da kolon MEVCUT) → verify ile çürütüldü
- **W5 supabase-agent N1+N2** (Tuna "low risk de bırakma"):
  - `is_employer_team_member(uuid)` helper fn (STABLE, SECURITY DEFINER)
  - `notes_select_team` policy sadeleştirildi (7 satır → 1)
  - `notes_delete_self_or_admin` notation explicit alias
- **W6 Codex T3 second-opinion:** %90 agreement, PASS
- **W7 auditor delta:** davranış parite + GRANT/REVOKE PASS
- **W8 APPLY remote:** SUCCESS + smoke 4 query PASS
  - Apply sırasında dependency ordering hatası yakalandı: helper fn section 5b'den 2b'ye taşındı (policy section 3'te kullanıyor)

### Apply Sonrası Live State
- `candidate_pipeline_state`: 0 row, RLS=on, 4 policy
- `employer_candidate_notes`: 0 row, RLS=on
- 3 RPC live: `hr_get_pipeline`, `hr_add_to_pipeline`, `hr_add_note`
- Helper fn: `is_employer_team_member(uuid)` STABLE, SECURITY DEFINER

### Original Detay
- **Sorun:** Sprint 7 migration `20260426012144_hr_pipeline_notes_mvp2.sql` LOCAL ONLY, remote'a apply olmamış. İçinde `position_id uuid REFERENCES positions(id)` ama `positions.id = bigint` → FK type mismatch.
- **Etkilenen yerler:** 4 nokta (`candidate_pipeline_state.position_id`, `hr_get_pipeline`, `hr_add_to_pipeline`, `hr_add_note`)
- **Kaynak:** 26 Nis öğle audit (CURRENT-STATE.md güncel hali, AUDIT bölümü)
- **Commit:** `4fd1dcd fix(supabase): Sprint 7 hr_pipeline_notes_mvp2 SQL fix + apply (Phase A) + docs`
